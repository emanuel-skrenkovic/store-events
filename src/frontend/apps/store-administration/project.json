{
  "root": "apps/store-administration",
  "sourceRoot": "apps/store-administration",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/store-administration",
        "outputPath": "dist/apps/store-administration"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "store-administration:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "store-administration:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "store-administration:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/store-administration"],
      "options": {
        "jestConfig": "apps/store-administration/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/store-administration/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
